<h1>Links</h1>

<ul id="links">
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL: <a href="<%= link.url %>"><%= link.url %></a>
      Tag: <%= link.tags.map(&:name).join(', ') %>
    </li>

  <% end %>
</ul>
